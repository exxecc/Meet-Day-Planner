import React from "react";
import ReactDOM from "react-dom/client";
import MeetDayCalc from "./meet-day-calculator";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <MeetDayCalc />
  </React.StrictMode>
);
